const mainRoutes = {
   main: "/",
 };
 
 const authRoutes = {
 };
 
 export const routes = { ...mainRoutes, ...authRoutes };
 
 const mainLinks = {
   main: () => "/",
 };
 
 export const links = { ...mainLinks };